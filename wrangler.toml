name = "restaurants-des-chefs"
compatibility_date = "2024-11-01"

# Cloudflare Pages exige cette clé pour appliquer ce fichier ; sortie Next `output: "export"`.
# Dashboard : Build command = `npm run build` (si les logs indiquent encore « No build command »).
pages_build_output_dir = "./out"
